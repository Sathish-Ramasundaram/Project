export default function HTML() {
  return (
    <div>
      <h2>HTML: The Skeleton of the Web</h2>
      <p>HTML (HyperText Markup Language) defines the structure of web pages.</p>
      <p>Every website starts with HTML — it's the foundation of everything.</p>
    </div>
  );
}
